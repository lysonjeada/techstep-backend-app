[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    unit: testes unitários, sem I/O externo
    integration: testes de integração via API (banco de teste real)
    security: testes de segurança (IDOR, auth, vazamento de dados)
    slow: testes mais lentos (ex.: smoke test de migrations)
